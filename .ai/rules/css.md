---
paths:
  - 'resources/css/**'
---

# Css

## Use app.css for global styling
Keep global design tokens, resets, accessibility focus styles, shared layout rules, and responsive foundations in resources/css/app.css. Use component sx or local styles for page-specific details instead of adding a parallel global styling system.
